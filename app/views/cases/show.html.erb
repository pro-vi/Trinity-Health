<h3>Patient Name: <%= @case.name %></h3>
<ul id="case_menu">

</ul>
<ul id="details">
  <p>Diagnosis: <%= @case.diagnosis %></p>
</ul>

<%= link_to "Back to case list", cases_path %>
<%= link_to "Edit", edit_case_path(@case) %>
<%= link_to "Delete", case_path(@case), method: :delete, data: {confirm: 'Are you sure?'} %>
